#include <stdio.h>

void int2bin(int num, char *binnum);
int bin2int(char *binnum);

int main(void)
{
	int num;
	unsigned char binNumber[32] = "00000000000000000000000000000000";
	printf("Enter a positive number. -1 to end\n");
	scanf("%d", num);
	while(num != -1){
		
		
	}
	
	return 0;
}